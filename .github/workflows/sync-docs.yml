name: sync-docs

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: List branches
        run: |
          git ls-remote --heads https://github.com/canonical/postgresql-operator
